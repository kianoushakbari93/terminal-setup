# =========================================
#  ~/.bash_profile - managed by terminal-setup
# =========================================
# Login bash shells read this file; source ~/.bashrc so interactive
# setup (ble.sh, starship, aliases) applies to login shells too.
[[ -r ~/.bashrc ]] && . ~/.bashrc
